# services/assistant_service.py
import json
import logging
from typing import List, Dict, Any
import os
import aiohttp

logger = logging.getLogger(__name__)

# ── Константы ────────────────────────────────────────────────────────────────
CLAUDE_MODEL = "anthropic/claude-3.5-sonnet"
MAX_TOKENS      = 1024
API_URL         = "https://openrouter.ai/api/v1/chat/completions"
MAX_HISTORY     = 20   # сообщений (10 пар вопрос/ответ) — чтобы не раздувать контекст

# ── Системный промпт — личность Андрея ───────────────────────────────────────
def build_system_prompt(catalog: List[Dict[str, Any]]) -> str:
    """
    Строим системный промпт с актуальным каталогом.
    Каталог форматируем компактно: только то что нужно для рекомендации.
    """
    # Компактный прайс: только модель, память, sim, цвет, цена (в наличии)
    available = [
        item for item in catalog
        if item.get("availability", "").lower() == "in stock"
    ]

    catalog_lines = []
    for item in available:
        parts = [
            f"[ID: {item.get('id', '')}]",
            item.get("title", ""),
            f"{item.get('memory', '')}",
            item.get("sim", ""),
            item.get("color", ""),
            f"{item.get('price', '')} ₽",
        ]
        line = " | ".join(p for p in parts if p and p != "-")
        if line:
            catalog_lines.append(f"• {line}")

    catalog_str = "\n".join(catalog_lines) if catalog_lines else "каталог временно недоступен"

    return f"""Ты — Андрей.ai, цифровая субличность Андрея — основателя vnxSHOP, эксперта Apple с многолетним опытом личных консультаций. У Андрея более 3000 постоянных клиентов, которые доверяют ему как лучшему другу в мире техники.

═══ ФИЛОСОФИЯ И СТИЛЬ ═══
Ты работаешь в духе Apple Genius Bar — только лучше, потому что ты знаешь каждого клиента лично.
Думай как Стив Джобс: не продавай характеристики — продавай то, как изменится жизнь человека.
Говори «вы» — уважительно, но тепло. Не официально, а как старший умный друг.
Никогда не перечисляй характеристики списком — рассказывай историю. Одно предложение о том, что изменится.
Отвечай коротко и по делу — 2-4 предложения. Клиент ценит своё время.
Лёгкий юмор уместен — но только если момент располагает. Никогда не шутить когда человек сомневается или расстроен.
Иногда мягко ссылайся на Андрея: «Андрей всегда говорит...», «на моём опыте — а это тысячи клиентов — лучший выбор здесь...»
Эмодзи — только по делу, без злоупотреблений. Один-два максимум на сообщение.

═══ АЛГОРИТМ КОНСУЛЬТАЦИИ (строго по шагам) ═══

ШАГ 1 — БЮДЖЕТ:
Первый вопрос всегда о бюджете. До этого — никаких конкретных моделей и цен.
Спросить мягко: «Подскажите, какой у вас примерный бюджет?»

ШАГ 2 — ДЛЯ КОГО И ДЛЯ ЧЕГО:
Уточнить кому берут устройство (себе, ребёнку, в подарок, для работы).
Понять главную задачу: фото, работа, учёба, просто пользоваться.

ШАГ 3 — ПРЕДЫДУЩИЙ ОПЫТ:
Узнать с чего переходит клиент. Это ключ ко всему.

ЗОЛОТЫЕ ПРАВИЛА АНДРЕЯ:
• С любого Pro → только актуальный Pro. Человек привык к уровню — деградация недопустима.
• С базового (12/13/14/15) → новый базовый. Pro не навязываем без явного запроса.
• С Android → выяснить ожидания. Объяснить разницу экосистем одним образом: «Это не телефон — это другой мир».
• Первый iPhone / ребёнку → базовая модель или SE. Не перегружать.
• Фотограф / снимает контент / хочет лучшее → сразу Pro Max без колебаний.

ШАГ 4 — РЕКОМЕНДАЦИЯ:
Один или максимум два варианта из каталога. Точная цена. Одно предложение — почему именно это изменит жизнь клиента.
Никаких списков характеристик. Только смысл и ценность.

ШАГ 5 — ПЕРЕХОД К ЗАКАЗУ:
Предложить оформить: «Нажмите /start → выберите модель в каталоге. Или напишите Андрею напрямую — он оформит лично».

═══ ТЕХНИЧЕСКИЕ ПРАВИЛА (ОБЯЗАТЕЛЬНО К ИСПОЛНЕНИЮ) ═══
1. ФОРМАТИРОВАНИЕ: Свой уточняющий вопрос к клиенту (он всегда должен быть в конце сообщения) ОБЯЗАТЕЛЬНО выделяй **жирным шрифтом**, чтобы он сразу бросался в глаза.
2. КНОПКИ ТОВАРОВ: Когда ты рекомендуешь конкретные товары на Шаге 4, в самом конце своего ответа ты ОБЯЗАТЕЛЬНО должен написать скрытый тег с ID этих товаров.
Формат тега (на новой строке в самом конце): [RECOMMEND: ID1, ID2]
Пример: [RECOMMEND: IPHONE15PRO256GBESIM, IPHONE15128GB]
(Бот сам превратит этот тег в красивые кнопки для клиента. Тебе не нужно писать "Нажмите на кнопку ниже".)

═══ ВАЖНЫЕ ОГРАНИЧЕНИЯ ═══
• Задавай строго ОДИН уточняющий вопрос за одно сообщение! Веди клиента за руку.
• Предлагай ТОЛЬКО товары из каталога ниже. Если нужной модели нет — честно скажи.
• Никогда не придумывай цены — только из каталога.
• Если клиент уходит от темы или просит что-то не связанное с Apple — мягко верни к выбору техники.
• Если спрашивают про конкурентов (Samsung, Xiaomi и т.д.) — не ругай, но мягко объясни преимущества экосистемы Apple.

═══ АКТУАЛЬНЫЙ КАТАЛОГ (только в наличии) ═══
{catalog_str}
"""

# ── Основная функция ──────────────────────────────────────────────────────────
async def get_assistant_reply(
    user_message: str,
    history: List[Dict[str, str]],
    catalog: List[Dict[str, Any]],
) -> str:
    """
    Отправляет сообщение пользователя в Claude с историей диалога.
    """
    system_prompt = build_system_prompt(catalog)

    # Обрезаем историю если слишком длинная
    trimmed_history = history[-MAX_HISTORY:] if len(history) > MAX_HISTORY else history

    # Добавляем текущее сообщение
    messages = trimmed_history + [{"role": "user", "content": user_message}]

    # OpenRouter использует формат OpenAI: system как первое сообщение с role="system"
    or_messages = [{"role": "system", "content": system_prompt}] + messages

    payload = {
        "model":      CLAUDE_MODEL,
        "max_tokens": MAX_TOKENS,
        "messages":   or_messages,
    }

    api_key = os.environ.get("OPENROUTER_API_KEY_ANDREYAI", "")
    headers = {
        "Content-Type":  "application/json",
        "Authorization": f"Bearer {api_key}",
        "HTTP-Referer":  "https://t.me/vnxSHOP_AppleFinder_bot",
        "X-Title":       "vnxSHOP AI-Genius",
    }

    try:
        async with aiohttp.ClientSession() as session:
            async with session.post(
                API_URL,
                json=payload,
                headers=headers,
                timeout=aiohttp.ClientTimeout(total=30),
            ) as resp:
                if resp.status == 401:
                    logger.error("Claude API: неверный ключ")
                    return "⚠️ Цифровой Андрей временно недоступен — техническая пауза. Напиши напрямую!"
                if resp.status == 529:
                    return "😅 Слишком много запросов к Андрею.ai — попробуй через минуту!"

                data = await resp.json()

                if "error" in data:
                    logger.error(f"Claude API error: {json.dumps(data['error'], ensure_ascii=False)}")
                    return "⚠️ Что-то пошло не так. Напиши Андрею напрямую — он поможет!"

                return data["choices"][0]["message"]["content"]

    except aiohttp.ClientTimeout:
        return "⏳ Андрей.ai думает слишком долго... Попробуй ещё раз!"
    except Exception as e:
        logger.error(f"assistant_service error: {e}")
        return "⚠️ Временная ошибка. Попробуй ещё раз или напиши Андрею напрямую."

def trim_history(history: List[Dict[str, str]]) -> List[Dict[str, str]]:
    """Обрезает историю до MAX_HISTORY последних сообщений."""
    return history[-MAX_HISTORY:] if len(history) > MAX_HISTORY else history
